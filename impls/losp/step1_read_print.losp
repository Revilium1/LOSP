import sys
sys.path.append("/workspaces/LOSP/impls/losp")

import reader
import printer

def READ(input):
	return(reader.read_str(input))

def EVAL(input):
	return(input)

def PRINT(input):
	return(printer.pr_str(input))

def REP(input):
	return(PRINT(EVAL(READ(input))))
while True:
    try:
        print(REP(input("user> ")))
    except EOFError:
        break
