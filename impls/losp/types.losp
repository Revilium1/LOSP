class LospSymbol:
    def __init__(self, name):
        self.name = name

    def __repr__(self):
        return f"Symbol({self.name})"

    def __eq__(self, other):
        return isinstance(other, MalSymbol) and self.name == other.name

    def __hash__(self):
        return hash(self.name)
